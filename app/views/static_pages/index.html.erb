<%= form_with url: root_path, method: :get do |f|%>
    <%= f.label :id%>
    <%= f.text_field :id%>
    <%= f.submit "Submit"%>
<% end %>

<% @photos.each do |p|%>
    <%= image_tag(Flickr.url_n(p)) %><br>
<% end %>